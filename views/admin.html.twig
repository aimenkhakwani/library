<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
    <meta charset="utf-8">
    <title>Epicodus Public Library</title>
  </head>
  <body>
    <div class="container">
      <h1>Epicodus Public Library</h1>
      <ul class="nav nav-tabs">
        <li role="presentation"><a href="/">Patron</a></li>
        <li role="presentation" class="active"class="active"><a href="/admin">Admin</a></li>
      </ul>
      <div class="row">
        <div class="col-xs-4">
          <h2>Add a Book</h2>
          <form action="/new_book" method="post">
            <label for="title">What is this books title?</label>
            <input id="title" type="text" name="title" class="form-control">
            <label for="genre">What genre is this book?</label>
            <input id="genre" type="text" name="genre" class="form-control">
            <label for="description">Enter a brief description of this book</label>
            <input id="description" type="text" name="description" class="form-control">
            <br>
            <button type="submit" class="btn btn-default">Add Book</button>
          </form>
        </div>
        <div class="col-xs-4">
          <h2>Search for Books</h2>
          <form action="/search_admin" method="post">
            <input name="search" placeholder="author name or book title" class="form-control">
            <br>
            <button class="btn btn-default" type="submit">Search!</button>
          </form>
          <br>
          {% if results is not empty %}
          <h3>Search Results:</h3>
          <ul>
              {% for result in results %}
                <li><a href="#">{{result.getTitle}}</a></li>
              {% endfor %}
          </ul>
          {% endif %}
        </div>
        <div class="col-xs-4">
          <h2>Books</h2>
          {% if books is not empty %}
          <ul>
              {% for book in books %}
                <li class="admin_full_list"><a href="/admin_book_page/{{book.getId}}">{{book.getTitle}}</a></li>
              {% endfor %}
          </ul>
          <a href="/delete_all_books">Delete All Books</a>
          {% endif %}
        </div>
      </div>
    </div>

  </body>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
</html>

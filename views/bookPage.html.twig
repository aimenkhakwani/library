<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
    <meta charset="utf-8">
    <title>Book Page</title>
  </head>
  <body>
    <div class="container">
      <h1><a href="/">Epicodus Public Library</a></h1>
      <ul class="nav nav-tabs">
        <li role="presentation" class="active"><a href="/patron/{{patron.getId}}">Patron</a></li>
        <li role="presentation"><a href="/admin/{{patron.getId}}">Admin</a></li>
      </ul>
      <div class="row">
        <div class="col-xs-3">
          <h5>Username: {{patron.getName}}</h5>
          <h5><a href="/">Log Out</a></h5> 
        </div>
        <div class="col-xs-9">

        </div>
      </div>
      <div class="row">
        <div class="col-xs-6">
          <h2>{{ book.getTitle }}:</h2>
          <h5>Genre: {{book.getGenre}} </h5>
          <h5>Description: {{book.getDescription}}</h5>
          {% if authors is not empty %}
            <h5>Authors:
            {% for author in authors %}
              <br>{{author.getName}}<br>
            {% endfor %}</h5>
          {% endif %}
        </div>
        <div class="col-xs-6">
          <h2>Checkout {{book.getTitle}}</h2>
          <form action="/checkout/{{book.getId}}/{{patron.getId}}" method="post">
            <button type="submit" class="btn btn-lg btn-success">Checkout</button>
          </form>
        </div>
      </div>
    </div>

  </body>
</html>

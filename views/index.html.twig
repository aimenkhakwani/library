<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
    <meta charset="utf-8">
    <title>Epicodus Public Library</title>
  </head>
  <body>
    <div class="container">
      <h1>Epicodus Public Library</h1>
      <ul class="nav nav-tabs">
        <li role="presentation" class="active"><a href="/">Patron</a></li>
        <li role="presentation"><a href="/admin">Admin</a></li>
      </ul>
      <div class="row">
        <div class="col-xs-7">
          <h2>Books</h2>
          {% if books is not empty %}
          <ul class="list-inline">
              {% for book in books %}
                <li><a href="#">{{book.getTitle}}</a></li>
              {% endfor %}
          </ul>
          {% endif %}
        </div>
        <div class="col-xs-5">
          <h2>Search for Books</h2>
          <form action="/search" method="post">
            <input name="search" placeholder="author name or book title" class="form-control">
            <br>
            <button class="btn btn-default" type="submit">Search!</button>
          </form>
          <br>
          {% if results is not empty %}
          <h3>Search Results:</h3>
          <ul>
              {% for result in results %}
                <li><a href="#">{{result.getTitle}}</a></li>
              {% endfor %}
          </ul>
          {% endif %}
        </div>
      </div>
    </div>

  </body>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
</html>
